<h1><%= @cocktail.name %></h1>
<h2><% @cocktail.doses.each do |dose| %></h2>
<h2><%= dose.description %> <%= dose.ingredient.name %> | <%= link_to 'Delete', dose_path(dose), method: "delete" %>
</h2>
<% end %>
<%= link_to "New Dose", new_cocktail_dose_path(@cocktail) %>

<%= link_to "Back to start", cocktails_path %>
